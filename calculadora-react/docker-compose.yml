services:
  calculadora-react:
    build: .
    container_name: calculadora-react
    ports:
      - "5998:5998"
      - "6006:6006"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true